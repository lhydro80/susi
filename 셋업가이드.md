# 📘 수시지원 입력 시스템 셋업 가이드 (정적 HTML 방식)

## 🎯 시스템 구조

```
[학생 PC/모바일]
  ↓ HTML + JSON 파일들 (학생 브라우저에 다운로드)
  ↓ 종속 드롭다운, 상세 정보 표시 (전부 브라우저에서, 즉시)
  ↓ [최종 제출] 클릭
  ↓ POST 요청
[Google Apps Script]
  ↓ Google Sheets에 자동 저장
[선생님: 통합 응답 시트 1개]
```

## 📦 받은 파일

```
webapp_static/
├── index.html            ← 학생용 메인 페이지
├── universities.json     ← 대학 목록 (5KB)
├── univ_filenames.json   ← 대학명 → 파일명 매핑 (8KB)
├── data/                 ← 대학별 데이터 (222개 파일)
│   ├── 강원대.json
│   ├── 건국대.json
│   └── ... (총 약 25MB)
├── receiver.gs           ← Apps Script 코드 (제출 받음)
└── 셋업가이드.md         ← 이 문서
```

## 🛠️ 셋업 단계

### 1단계: Google Sheets + Apps Script 만들기

```
1) https://sheets.google.com 접속
2) "빈 스프레드시트" → 제목 "2026 수시지원 응답"
3) 메뉴: 확장 프로그램 → Apps Script
4) 기본 코드 모두 삭제
5) receiver.gs 내용 전체 복사 → 붙여넣기
6) Ctrl+S 저장 (프로젝트 이름은 아무거나)
```

### 2단계: Apps Script 웹앱 배포

```
1) 우측 상단 "배포" → "새 배포"
2) ⚙️ → "웹 앱"
3) 설정:
   - 다음 사용자 인증으로 실행: 나
   - 액세스 권한이 있는 사용자: 모든 사용자
4) "배포" → 권한 승인 (고급 → 안전하지 않은 페이지로 이동 → 허용)
5) ⭐ 웹 앱 URL 복사 (예: https://script.google.com/macros/s/AKfyc.../exec)
```

### 3단계: index.html에 Apps Script URL 입력

`index.html` 파일을 메모장이나 코드 에디터로 열기:

```javascript
// 약 70번째 줄 근처에 있음
const CONFIG = {
  DATA_BASE: './',
  
  // ⚠️ 이 줄의 'YOUR_APPS_SCRIPT_WEB_APP_URL_HERE' 부분을 
  // 위에서 복사한 URL로 변경
  SUBMIT_URL: 'YOUR_APPS_SCRIPT_WEB_APP_URL_HERE',
  
  REQUIRED_SLOTS: 6,
  OPTIONAL_SLOTS: 4,
  DEADLINE: '2025-12-31 23:59',  // ⚠️ 마감일 변경
  NOTICE: '...'                    // ⚠️ 공지 변경
};
```

저장.

### 4단계: 호스팅 (학생들이 접속할 곳)

선택지 3가지:

#### 옵션 A: GitHub Pages (무료, 추천)

```
1) GitHub 가입 (github.com)
2) 새 저장소 만들기:
   - Repository name: 본인이름-susi (예: hong-susi)
   - Public 선택
   - "Add README" 체크
3) 업로드:
   - "Add file" → "Upload files"
   - webapp_static 폴더의 모든 파일을 업로드:
     · index.html
     · universities.json
     · univ_filenames.json
     · data/ 폴더 (222개 파일)
4) Settings → Pages:
   - Source: Deploy from a branch
   - Branch: main, / (root)
   - Save
5) 몇 분 후 URL 생성:
   https://본인깃허브id.github.io/저장소이름/
   
   이 URL이 학생들에게 줄 링크!
```

#### 옵션 B: Google Drive 호스팅

```
1) Google Drive에 폴더 만들기 "수시지원"
2) webapp_static의 모든 파일/폴더 업로드
3) 폴더 공유 → "링크가 있는 모든 사용자"
4) index.html 우클릭 → "미리보기" → URL 복사
   ⚠️ Drive 호스팅은 종종 작동 안 함. GitHub Pages 추천.
```

#### 옵션 C: Netlify (가장 간단)

```
1) netlify.com 가입
2) "Add new site" → "Deploy manually"
3) webapp_static 폴더 통째로 끌어다 놓기
4) URL 자동 생성: https://random-name.netlify.app
```

### 5단계: 테스트

```
1) 호스팅 URL을 시크릿 창에서 열기
2) 학번 99999 + 비밀번호 test1234 → [처음 등록]
3) 메인 화면 즉시 표시되는지 확인
4) 1번 슬롯 대학 선택 → 잠시 후 (그 대학 데이터 다운) 전형 표시
5) 6개 필수 슬롯 채우기
6) [최종 제출] 클릭
7) Google Sheets로 가서 responses 시트 확인 → 데이터 들어왔는지

✅ 데이터 들어오면 성공
❌ 안 들어오면 → Apps Script URL 잘못 입력했을 가능성
```

### 6단계: 학생에게 안내

```
[수시지원 입력 안내]

링크: [GitHub Pages URL 또는 Netlify URL]

📋 방법:
- 처음 사용: 학번(5자리) + 비밀번호 설정 → [처음 등록]
- 재접속: 학번 + 비밀번호 → [로그인]
- 대학·전형·모집단위 선택 시 자동 정보 표시
- 필수 6개 + 선택 4개
- [임시저장]: 브라우저에 임시 저장 (본인 PC에만)
- [최종 제출]: 선생님에게 전송

⏰ 마감일: ___년 ___월 ___일 ___시
```

## 📥 학생 응답 회수

학생들이 [최종 제출]하면 Google Sheets에 자동 저장됩니다:

```
responses 시트:
학번    | 슬롯 | 대학명   | 세부전형      | 모집단위  | 제출시각      | 수정시각
30601   | 1   | 고려대   | 학생부종합... | 통계학과  | 2025-12-01... | 2025-12-01...
30601   | 2   | 성균관대 | ...           | ...       | ...           | ...
30602   | 1   | ...
```

엑셀로 가져오기:
1. Google Sheets에서 데이터 전체 선택 → 복사
2. 엑셀 수시지원계획서 시트로 가서
3. D2 (학번) 컬럼부터 붙여넣기
4. 자동 lookup이 53개 정보 채워줌

## 🔧 매년 사용

다음 해에도 사용하려면:

1. 새 db파일.xlsx 받기 (2027학년도 등)
2. db_to_json.py 같은 변환 도구로 다시 JSON 생성
3. GitHub Pages의 data/ 폴더 교체
4. index.html에서 마감일 등 업데이트
5. 끝

## ⚠️ 자주 묻는 문제

**Q: 학생이 첫 화면이 안 떠요**
- HTML 파일 위치 확인
- 브라우저 콘솔(F12) 빨간색 오류 확인
- universities.json 파일이 같은 폴더에 있는지

**Q: 제출했는데 시트에 안 들어옴**
- Apps Script URL이 index.html에 정확히 들어갔는지
- 배포 설정 "모든 사용자" 확인
- Apps Script 권한 재승인

**Q: 대학 데이터가 안 떠요**
- data/ 폴더 안에 222개 파일이 모두 업로드 됐는지
- 파일명에 한글 들어가는데, GitHub Pages는 UTF-8 한글 지원함

**Q: 보안이 걱정돼요**
- 비밀번호는 학생 브라우저에만 저장 (LocalStorage)
- 학생들끼리는 서로의 데이터 못 봄
- responses 시트는 Apps Script 통해서만 접근, 학생들은 못 봄
- db는 공개 정보이므로 노출되어도 문제 없음

## 📞 도움

- 학생들이 사용 중 막힐 때: 본 가이드의 "자주 묻는 문제" 참고
- 코드 수정 필요 시: Claude에게 문의
